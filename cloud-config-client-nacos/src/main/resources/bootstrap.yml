server:
  port: 9003


spring:
  profiles:
    active: dev
  application:
    name: nacos-conifg-client
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
